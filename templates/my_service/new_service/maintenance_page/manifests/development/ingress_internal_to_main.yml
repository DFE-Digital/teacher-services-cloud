apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: calculate-teacher-pay-development.test.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: calculate-teacher-pay-development.test.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: calculate-teacher-pay-development
            port:
              number: 80
