apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: development.calculate-teacher-pay.education.gov.uk
spec:
  ingressClassName: nginx
  rules:
  - host: development.calculate-teacher-pay.education.gov.uk
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: calculate-teacher-pay-development
            port:
              number: 80
