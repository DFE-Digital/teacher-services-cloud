apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: calculate-teacher-pay-maintenance.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: calculate-teacher-pay-maintenance.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: calculate-teacher-pay-maintenance
            port:
              number: 80
