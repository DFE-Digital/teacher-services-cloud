apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: calculate-teacher-pay-temp.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: calculate-teacher-pay-temp.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: calculate-teacher-pay-production
            port:
              number: 80
